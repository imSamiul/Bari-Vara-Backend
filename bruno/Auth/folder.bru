meta {
  name: Auth
}

auth {
  mode: none
}

docs {
  Registration, email OTP, login (password or Google ID token), refresh rotation, and password reset.

  Cookies set on success: `bv_access`, `bv_refresh`.
}

meta {
  name: Refresh
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}{{apiPrefix}}/auth/refresh
  body: none
  auth: none
}

docs {
  # Refresh

  Rotates the refresh token. Sends the current `bv_refresh` cookie.
  Replaying an old refresh cookie returns `REFRESH_TOKEN_REUSED` and kills the session family.
}

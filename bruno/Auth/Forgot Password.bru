meta {
  name: Forgot Password
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}{{apiPrefix}}/auth/forgot-password
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "{{tenantEmail}}"
  }
}

docs {
  # Forgot Password

  Emails a reset OTP (same Redis OTP mechanism as email verify).
}
